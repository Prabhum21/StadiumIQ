[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "C90", "N", "B", "UP", "ARG", "PTH", "SIM", "RUF"]
ignore = ["E501", "ARG001"]
